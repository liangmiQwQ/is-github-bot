{
  "name": "@is-github-bot/monorepo",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "scripts": {
    "is-github-bot": "vp run is-github-bot#is-github-bot",
    "ready": "vp check && vp run -r test && vp run -r build",
    "dev": "vp run website#dev",
    "build": "vp run -r build",
    "release": "bumpp"
  },
  "devDependencies": {
    "@oxc-node/cli": "catalog:cli",
    "bumpp": "catalog:cli",
    "vite-plus": "catalog:build"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "packageManager": "pnpm@11.4.0"
}
